volumes:
  uploads_data:

services:

  zookeeper:
    image: confluentinc/cp-zookeeper:7.7.0
    healthcheck:
      test: echo stat | nc localhost 9006
      interval: 10s
      timeout: 5s
      retries: 5
    environment:
      ZOOKEEPER_CLIENT_PORT: 9006
      ZOOKEEPER_TICK_TIME: 2000

  kafka:
    image: confluentinc/cp-kafka:7.7.0
    depends_on:
      zookeeper:
        condition: service_healthy
    ports:
      - "9004:9004"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "kafka-topics --bootstrap-server localhost:9004 --list > /dev/null 2>&1",
        ]
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 30s
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:9006
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9004
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9004
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

  kafbat-ui:
    image: ghcr.io/kafbat/kafka-ui:latest
    depends_on:
      kafka:
        condition: service_healthy
    ports:
      - "9005:9005"
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9004

  webhook-receiver:
    build: .
    ports:
      - "9003:9003"
    command: uvicorn app.webhook_receiver:app --host 0.0.0.0 --port 9003
    volumes:
      - uploads_data:/app/uploads

  api:
    build: .
    depends_on:
      kafka:
        condition: service_healthy
    ports:
      - "9002:9002"
    env_file: .env
    environment:
      KAFKA_BOOTSTRAP_SERVERS: kafka:9004
      TEST_WEBHOOK_URL: http://webhook-receiver:9003/webhook-files
    volumes:
      - uploads_data:/app/uploads

  worker:
    build: .
    depends_on:
      kafka:
        condition: service_healthy
    command: python -m app.worker
    env_file: .env
    environment:
      KAFKA_BOOTSTRAP_SERVERS: kafka:9004
    volumes:
      - uploads_data:/app/uploads
    restart: unless-stopped
